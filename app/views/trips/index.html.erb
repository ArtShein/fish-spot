<p style="color: green"><%= notice %></p>

<% content_for :title, "Trips" %>

<h1><%= t('.title') %></h1>

<div id="trips">
  <% @trips.each do |trip| %>
    <%= render trip %>
    <p>
      <%= link_to t('.show_trip'), trip %>
    </p>
  <% end %>
</div>

<%= link_to t('.new_trip'), new_trip_path %>
