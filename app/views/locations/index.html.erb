<p style="color: green"><%= notice %></p>

<% content_for :title, "Locations" %>

<h1><%= t('.title') %></h1>

<div id="locations">
  <% @locations.each do |location| %>
    <%= render location %>
    <p>
      <%= link_to t('.show_location'), location %>
    </p>
  <% end %>
</div>

<%= link_to t('.new_location'), new_location_path %>
