<p style="color: green"><%= notice %></p>

<%= render @location %>

<div>
  <%= link_to t('.edit_location'), edit_location_path(@location) %> |
  <%= link_to t('locations.back'), locations_path %>

  <%= button_to t('.destroy_location'), @location, method: :delete %>
</div>
